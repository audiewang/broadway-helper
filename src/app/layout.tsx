import type { Metadata } from 'next';
import { Inter } from 'next/font/google';
import './globals.css';

const inter = Inter({ subsets: ['latin'] });

export const metadata: Metadata = {
  title: 'Broadway Price Tracker - Find Hidden Deals & Pricing Anomalies',
  description: 'Compare Broadway ticket prices across all sources. Discover when Row B is $200 while others are $500+. Avoid resale markups.',
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en">
      <body className={inter.className}>{children}</body>
    </html>
  );
}